Wallpaper 1:

//your parameter variables go here!
  let pX = 200; // changes the x location of the panda
  let pY = 200; // changes the y location of the panda

  let bX = 250; // changes the x location of the bamboo
  let bY = 0; // changes the y location of the bamboo

  let lX = bX + 2 // moves the leaf's x location with the bamboo shoot 
  let lY = bY + 27 // moves the leaf's y location with the bamboo shoot 

  let sX = 200;
  let sY = 200;
  let Size = 10;

  let hX = 50;
  let hY = 80;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); // set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 400;
  pWallpaper.grid_settings.cell_height = 400;
  pWallpaper.grid_settings.row_offset  = 50;
}
/////////////////////////////////

Wallpaper 2:

//your parameter variables go here!
  let pX = 50; // changes the x location of the panda
  let pY = 35; // changes the y location of the panda

  let bX = 280; // changes the x location of the bamboo
  let bY = 21; // changes the y location of the bamboo

  let lX = bX + 2 // moves the leaf's x location with the bamboo shoot 
  let lY = bY + 27 // moves the leaf's y location with the bamboo shoot 

  let sX = 200;
  let sY = 200;
  let Size = 10;

  let hX = 50;
  let hY = 280;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_PORTRAIT);
  pWallpaper.show_guide(false); // set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 400;
  pWallpaper.grid_settings.cell_height = 400;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 3:

//your parameter variables go here!
  let pX = 50; // changes the x location of the panda
  let pY = 200; // changes the y location of the panda

  let bX = 50; // changes the x location of the bamboo
  let bY = 0; // changes the y location of the bamboo

  let lX = bX + 2 // moves the leaf's x location with the bamboo shoot 
  let lY = bY + 27 // moves the leaf's y location with the bamboo shoot 

  let sX = 200;
  let sY = 150;
  let Size = 40;

  let hX = 150;
  let hY = 80;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); // set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 400;
  pWallpaper.grid_settings.cell_height = 400;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 4:

//your parameter variables go here!
  let pX = 50; // changes the x location of the panda
  let pY = 200; // changes the y location of the panda

  let bX = 50; // changes the x location of the bamboo
  let bY = 450; // changes the y location of the bamboo

  let lX = bX + 2 // moves the leaf's x location with the bamboo shoot 
  let lY = bY + 27 // moves the leaf's y location with the bamboo shoot 

  let sX = 200;
  let sY = 150;
  let Size = 10;

  let hX = 150;
  let hY = 80;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); // set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 400;
  pWallpaper.grid_settings.cell_height = 400;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 5:

//your parameter variables go here!
  let pX = 100; // changes the x location of the panda
  let pY = 190; // changes the y location of the panda

  let bX = 100; // changes the x location of the bamboo
  let bY = 0; // changes the y location of the bamboo

  let lX = bX + 2 // moves the leaf's x location with the bamboo shoot 
  let lY = bY + 27 // moves the leaf's y location with the bamboo shoot 

  let sX = 150;
  let sY = 150;
  let Size = 50;

  let hX = 200;
  let hY = 80;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); // set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 300;
  pWallpaper.grid_settings.cell_height = 300;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 6:

//your parameter variables go here!
  let pX = 150; // changes the x location of the panda
  let pY = 250; // changes the y location of the panda

  let bX = 100; // changes the x location of the bamboo
  let bY = 0; // changes the y location of the bamboo

  let lX = bX + 2 // moves the leaf's x location with the bamboo shoot 
  let lY = bY + 27 // moves the leaf's y location with the bamboo shoot 

  let sX = 150;
  let sY = 150;
  let Size = 50;

  let hX = 200;
  let hY = 80;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); // set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 500;
  pWallpaper.grid_settings.cell_height = 500;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 7:

//your parameter variables go here!
  let pX = 150; // changes the x location of the panda
  let pY = 250; // changes the y location of the panda

  let bX = 90; // changes the x location of the bamboo
  let bY = 250; // changes the y location of the bamboo

  let lX = bX + 2 // moves the leaf's x location with the bamboo shoot 
  let lY = bY + 27 // moves the leaf's y location with the bamboo shoot 

  let sX = 220;
  let sY = 210;
  let Size = 50;

  let hX = 200;
  let hY = 80;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GRID_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); // set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 500;
  pWallpaper.grid_settings.cell_height = 500;
  pWallpaper.grid_settings.row_offset  = 50;
}


/////////////////////////////////

Wallpaper 8:

//your parameter variables go here!
  let pX = 150; // changes the x location of the panda
  let pY = 250; // changes the y location of the panda

  let bX = 90; // changes the x location of the bamboo
  let bY = 0; // changes the y location of the bamboo

  let lX = bX + 2 // moves the leaf's x location with the bamboo shoot 
  let lY = bY + 27 // moves the leaf's y location with the bamboo shoot 

  let sX = 220;
  let sY = 210;
  let Size = 30;

  let hX = 200;
  let hY = 80;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); // set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 300;
  pWallpaper.grid_settings.cell_height = 300;
  pWallpaper.grid_settings.row_offset  = 50;
}

/////////////////////////////////

Wallpaper 9:

//your parameter variables go here!
  let pX = 50; // changes the x location of the panda
  let pY = 50; // changes the y location of the panda

  let bX = 160; // changes the x location of the bamboo
  let bY = 0; // changes the y location of the bamboo

  let lX = bX + 2 // moves the leaf's x location with the bamboo shoot 
  let lY = bY + 27 // moves the leaf's y location with the bamboo shoot 

  let sX = 220;
  let sY = 210;
  let Size = 30;

  let hX = 200;
  let hY = 80;

function setup_wallpaper(pWallpaper) {
  pWallpaper.output_mode(GLIDE_WALLPAPER);
  pWallpaper.resolution(NINE_LANDSCAPE);
  pWallpaper.show_guide(false); // set this to false when you're ready to print

  //Grid settings
  pWallpaper.grid_settings.cell_width  = 300;
  pWallpaper.grid_settings.cell_height = 300;
  pWallpaper.grid_settings.row_offset  = 50;
}
